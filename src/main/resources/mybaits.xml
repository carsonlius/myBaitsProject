<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
<!--     指定配置文件路径-->
    <properties resource="jdbc.propertires"></properties>

    <settings>
        <setting name="logImpl" value="STDOUT_LOGGING"></setting>
    </settings>
    <typeAliases>
<!--        <typeAlias type="com.carsonlius.entries.SystemLogDetails" alias="SystemLogDetails"></typeAlias>-->
        <package name="com.carsonlius.entries"></package>
    </typeAliases>
    <environments default="mac">
        <environment id="mac">
            <transactionManager type="JDBC"/>
            <dataSource type="POOLED">
                <property name="driver" value="${jdbc.driver}"/>
                <property name="url" value="${jdbc.url}"/>
                <property name="username" value="${jdbc.username}"/>
                <property name="password" value="${jdbc.password}"/>
            </dataSource>
        </environment>
    </environments>
    <mappers>
<!--        <mapper resource="com/carsonlius/dao/SystemLogDetailsDao.xml"/>-->
        <package name="com.carsonlius.dao"></package>
    </mappers>
</configuration>